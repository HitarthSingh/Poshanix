# Example environment variables for the server
# Copy this file to `.env` in the `server/` folder and fill values before deploying.

# API key for the AI provider (Gemini or OpenAI)
GEMINI_API_KEY=

# Set to `google` to use Google Generative Language (Gemini). Otherwise server uses OpenAI-compatible endpoint.
GEMINI_API_TYPE=google

# Model name to use (optional). Examples: `gemini-2.5-flash` or `gpt-4o-mini` or `gpt-4` depending on provider.
GEMINI_MODEL=models/gemini-2.5-flash

# Optional: override the API endpoint. If empty, a default OpenAI-compatible endpoint is used.
GEMINI_API_ENDPOINT=

# Server port (Render will set this automatically when deployed)
PORT=3001
GEMINI_API_KEY=AI-xxxxxxxxxxxxxxxx